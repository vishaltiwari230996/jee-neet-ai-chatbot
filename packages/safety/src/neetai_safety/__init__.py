"""Safety guardrails.

Phase 5 will add:
    * `input_check.py`  — distress + abuse detection on incoming doubt
    * `output_check.py` — banned-phrase + false-guarantee detection + citation verifier
    * `escalation.py`   — rules from blueprint §14 → escalation events
"""

__version__ = "0.1.0"
